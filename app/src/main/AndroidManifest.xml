<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="acad277.stanfield.ben.resumobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LinkedIn_data" />
        <activity android:name=".Job_add" />
        <activity android:name=".Cover_Letter_Edit" />
        <activity android:name=".Job_Delete" />
        <activity android:name=".Job_edit" />
        <activity android:name=".Resume_view" />
        <activity android:name=".Preferences"></activity>
    </application>

</manifest>